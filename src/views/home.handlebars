<h1>Home</h1>

<div>
    {{#each productos}}
        <div class="card">
            <h2> {{this.title}} </h2>
            <p> {{this.description}}  </p>
            <p> {{this.price}} </p>
        </div>
    {{/each}}
</div>